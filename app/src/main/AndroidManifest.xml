<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.menu">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/kh_logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/kh_logo_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Menu">
        <activity android:name=".MiniActivities"/>
        <activity android:name=".KeyboardStuff"/>
        <activity android:name=".Alerts"/>
        <activity android:name=".DroidCafe"/>
        <activity android:name=".OrderActivity"/>
        <activity android:name=".MainMenu">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>